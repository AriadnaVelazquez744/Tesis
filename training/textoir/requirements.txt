# TEXTOIR Server dependencies
# Install on toothless (server) for both training and API serving

# API server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Model inference (used by TEXTOIR backbones)
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
scikit-learn>=1.2.0
pandas>=1.5.0
tqdm>=4.60.0
scipy>=1.10.0
easydict>=1.9
accelerate>=0.30

# HTTP client (local machine side)
requests>=2.28.0
